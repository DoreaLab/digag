# Drone/config/config.yaml
path: /content/digag/Drone/Datasets   # raiz do dataset
train: train/images                                          # usa /images e /labels
val: valid/images                                            # valida nos mesmos dados
names:
  0: cow

